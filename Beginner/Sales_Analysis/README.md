# Sales Data Analysis (Basic version)

A basic project to learn Pandas, NumPy, and Matplotlib.

## How to Run:
1. Install requirements: `pip install pandas numpy matplotlib`
2. Run code: `python main.py`