# Combined Product Analysis (Intermediate version)

A basic project to learn Pandas DataFrame concatenation, missing value treatment, and comparative plotting using Matplotlib.

## How to Run:
1. Install requirements: `pip install pandas numpy matplotlib`
2. Run code: `python main.py`